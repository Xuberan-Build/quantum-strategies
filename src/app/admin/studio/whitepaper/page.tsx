import { StudioFormatList } from '@/components/admin/studio/StudioFormatList';

export default function WhitepaperStudioPage() {
  return <StudioFormatList format="whitepaper" formatLabel="Whitepaper" formatColor="#f59e0b" />;
}
